rules_version = '2';

service cloud.firestore {
  match /databases/kenyasales/documents {
    match /programs/{programId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
