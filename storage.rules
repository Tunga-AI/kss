rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Temporary: allow all reads and writes until 2026-03-01
      allow read, write: if request.time < timestamp.date(2026, 3, 1);
    }
  }
}
