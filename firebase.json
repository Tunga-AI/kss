{
  "firestore": [
    {
      "rules": "firestore.rules",
      "database": "(default)"
    },
    {
      "rules": "firestore.kenyasales.rules",
      "database": "kenyasales"
    }
  ],
  "storage": {
    "rules": "storage.rules"
  }
}
